import 'package:flutter/material.dart';

class ProPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pro Page'),
      ),
      body: Center(
        child: Text('This is the Pro Page'),
      ),
    );
  }
}